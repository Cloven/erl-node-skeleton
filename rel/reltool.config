%% -*- mode: erlang -*-
%% ex: ft=erlang
{sys, [
       {lib_dirs, ["../apps", "../deps"]},
       {erts, [{mod_cond, derived}, {app_file, strip}]},
       {app_file, strip},
       {rel, "skelnode", "1",
        [
         kernel,
         stdlib,
         sasl,
         crypto,
         ranch,
         cowboy,
         skelnode,
         skel
        ]},
       {rel, "start_clean", "",
        [
         kernel,
         stdlib
        ]},
       {boot_rel, "skelnode"},
       {profile, embedded},
       {incl_cond, derived},
       {excl_archive_filters, [".*"]}, %% Do not archive built libs
       {excl_sys_filters, ["^bin/.*", "^erts.*/bin/(dialyzer|typer)",
                           "^erts.*/(doc|info|include|lib|man|src)"]},
       {excl_app_filters, ["\.gitignore"]},
       {app, ranch, [{mod_cond, app}, {incl_cond, include}]},
       {app, cowboy, [{mod_cond, app}, {incl_cond, include}]},
       {app, skelnode, [{mod_cond, app}, {incl_cond, include}]},
       {app, skel, [{mod_cond, app}, {incl_cond, include}]}
      ]}.

{target_dir, "skelnode"}.

{overlay, [
           {mkdir, "log/sasl"},
           {copy, "files/erl", "\{\{erts_vsn\}\}/bin/erl"},
           {copy, "files/nodetool", "\{\{erts_vsn\}\}/bin/nodetool"},
           {copy, "files/skelnode", "bin/skelnode"},
           {copy, "files/skelnode.cmd", "bin/skelnode.cmd"},
           {copy, "files/start_erl.cmd", "bin/start_erl.cmd"},
           {copy, "files/install_upgrade.escript", "bin/install_upgrade.escript"},
           {copy, "files/sys.config", "releases/\{\{rel_vsn\}\}/sys.config"},
           {copy, "files/vm.args", "releases/\{\{rel_vsn\}\}/vm.args"}
          ]}.
